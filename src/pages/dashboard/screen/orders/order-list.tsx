import { Order } from "~/apis/order/order.interface.api";

const ListOrder: React.FC<{ onEdit: (order: Order) => void }> = ({ onEdit }) => {
  return <div>List Order Component</div>;
}
export default ListOrder;